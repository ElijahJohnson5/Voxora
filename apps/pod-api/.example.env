# ---- Pod API Configuration ----

# [Required] PostgreSQL connection string
DATABASE_URL=postgresql://voxora:voxora@localhost:5432/pod

# [Required] Hub API origin this Pod is registered with
HUB_URL=http://localhost:4001

# [Required] This Pod's registered ID on the Hub
# Run `cargo run -p pod-api --bin pod-setup` to generate these credentials
POD_ID=
POD_CLIENT_ID=
POD_CLIENT_SECRET=

# [Optional] HTTP server port (default: 4002)
PORT=4002

# [Optional] Rust log filter (default: none)
RUST_LOG=pod_api=debug,tower_http=debug
